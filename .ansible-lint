---

exclude_paths:
  - .cache/
  - .github/
  - roles/requirements.yaml
  - vars/vault.yaml

warn_list:
  - internal-error
  # Role name {} does not match ``^[a-z][a-z0-9_]+$`` pattern
  - '106'

# vi: ft=yaml
