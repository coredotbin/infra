---
all:
  hosts:
    cb-rt-01:
    hypervisor:
    pihole:
    nextcloud:
    db:
    lin1:
  children:
    ios:
      hosts:
        cb-rt-01:
    debian:
      hosts:
        hypervisor:
        pihole:
        nextcloud:
        db:
    docker_hosts:
      hosts:
        hypervisor:
    dhcp:
      hosts:
        pihole:
    dbservers:
      hosts:
        db:
    vpnservers:
      hosts:
        lin1:
    webservers:
      hosts:
        pihole:
        nextcloud:
